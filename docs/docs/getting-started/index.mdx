---
sidebar_position: 0
title: Getting Started
slug: /getting-started/
---

# Install a tier, and get a plugin running

The two runtimes are installed and used independently — a frontend with no
server takes only the first, a service with no browser only the second — and an
application that has both declares the relationship rather than discovering it.

import DocCardList from '@theme/DocCardList';

<DocCardList />
